export enum TicketStatus {
  Escalated = 'escalated',
  Resolved = 'resolved',
  InProgress = 'in_progress',
  Closed = 'closed',
}

export enum ServicomCategory {
  COMPLAINT = 'complaint',
  FEEDBACK = 'feedback',
  ENQUIRY = 'enquiry',
}
